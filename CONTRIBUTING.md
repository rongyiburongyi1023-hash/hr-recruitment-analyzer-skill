# 贡献指南

感谢你参与改进 HR Recruitment Analyzer Skill。请保持贡献聚焦于招聘分析方法、专业 Prompt、工作模板和匿名化案例，不添加前端、后端、数据库或外部服务依赖。

## 贡献招聘案例

- 在 `examples/` 中新增 Markdown 文件，使用清晰、描述性的 snake_case 文件名。
- 按“输入 → Skill 处理 → 输出结果”的结构编写。
- 明确说明案例是虚构、教学用途或已完成不可逆匿名化。
- 输出应体现业务目标、关键产出、岗位能力和评价证据之间的关系。
- 对未确认的指标或组织信息标记为假设或待确认项，不补造事实。

## 新增或修改 Prompt

- 将任务专属指引放入 `prompts/`，避免把普通聊天提示当作专业 Prompt。
- 至少包含角色定义、输入信息、分析步骤、输出格式和注意事项。
- 保持与 `SKILL.md` 的任务路由、使用规则和限制条件一致。
- 新增任务类型时，说明适用边界，并在 `SKILL.md` 中提供可发现的入口。

## 新增或修改模板

- 将可直接用于 HR 工作的模板放入 `templates/`。
- 字段应支持记录事实、证据、假设和待确认项。
- 避免加入未经验证的固定指标、法务结论或组织专属政策。
- 保持 Markdown 格式简洁，便于复制、填写和审阅。

## 安全与隐私

禁止提交：

- 真实候选人的姓名、联系方式、简历、面试记录或其他个人信息。
- 未经授权的企业名称、招聘计划、薪酬数据、绩效数据或其他企业机密。
- API Key、Access Token、密码、私钥、Cookie、`.env` 文件或其他凭据。
- 基于与岗位无关的敏感属性进行筛选或评价的内容。

如需展示真实工作方法，请使用虚构数据或完成不可逆匿名化，并在提交前再次检查 Git diff。

## 提交流程

1. 创建独立分支并完成修改。
2. 检查 Markdown 链接、文件命名和敏感信息。
3. 确认没有修改与贡献无关的文件。
4. 提交 Pull Request，简要说明变更目的、适用场景和验证方式。
